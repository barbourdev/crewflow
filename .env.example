# CrewFlow Configuration

# AI Providers (at least one required)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Database (default: local SQLite)
DATABASE_URL="file:./data/app.db"

# Encryption key for API keys stored in DB (auto-generated if not set)
ENCRYPTION_KEY=

# Server
PORT=3000
NODE_ENV=development
